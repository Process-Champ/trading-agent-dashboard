name: Run Momentum Trading Scanner

on:
  workflow_dispatch: # Allows you to trigger it via GitHub API or manual button
  repository_dispatch:
    types: [external_clock_trigger] # Allows external cron apps to trigger it via Webhook

jobs:
  scan-market:
# ... rest of your workflow steps remain exactly the same
